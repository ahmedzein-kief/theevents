enum VendorSettingType { store, taxInfo, payoutInfo }

enum VendorProductType { physical, digital, none }

/// when creating product can be of type physical or digital or package

enum SeoIndexType {
  // ignore: constant_identifier_names
  INDEX,
  // ignore: constant_identifier_names
  NON_INDEX,
}

enum RequestType {
  // ignore: constant_identifier_names
  CREATE,
  // ignore: constant_identifier_names
  UPDATE,
}
